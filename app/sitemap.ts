import type {MetadataRoute} from "next";import {demoPosts} from "@/lib/content";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??"https://fullstack-cloudflare-supabase-starter.sites.openai.com";const routes=["","/nosotros","/servicios","/noticias","/blog","/faq","/legal/privacidad","/legal/terminos"];return[...routes.map(route=>({url:`${base}${route}`,lastModified:new Date(),changeFrequency:route===""?"weekly" as const:"monthly" as const,priority:route===""?1:.7})),...demoPosts.map(post=>({url:`${base}/blog/${post.slug}`,lastModified:new Date(post.published_at!),changeFrequency:"monthly" as const,priority:.65}))]}
