"use client";
import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {getSupabaseClient,isSupabaseConfigured} from "@/lib/supabase/client";

// Formulario de acceso: Supabase emite y renueva la sesión; nunca guarda contraseñas en la aplicación.
export default function LoginPage(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);useEffect(()=>{getSupabaseClient()?.auth.getSession().then(({data})=>{if(data.session)router.replace("/admin")})},[router]);async function submit(event:FormEvent){event.preventDefault();const client=getSupabaseClient();if(!client){setError("Configura las variables NEXT_PUBLIC_SUPABASE_* para habilitar el acceso.");return}setLoading(true);setError("");const{error}=await client.auth.signInWithPassword({email,password});setLoading(false);if(error){setError("No fue posible iniciar sesión. Revisa tus credenciales.");return}router.replace("/admin")}
return <main className="admin-shell"><section className="admin-card"><p className="eyebrow">Administración privada</p><h1>Iniciar sesión</h1><p className="article-meta">Acceso exclusivo para editores autorizados.</p>{!isSupabaseConfigured()&&<p className="notice">Modo demostración: falta conectar Supabase.</p>}<form className="form-stack" onSubmit={submit}><div className="form-field"><label htmlFor="email">Correo</label><input className="input" id="email" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="form-field"><label htmlFor="password">Contraseña</label><input className="input" id="password" type="password" autoComplete="current-password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div>{error&&<p role="alert" className="notice">{error}</p>}<button className="button button-primary" disabled={loading}>{loading?"Verificando…":"Entrar al panel"}</button></form></section></main>}
