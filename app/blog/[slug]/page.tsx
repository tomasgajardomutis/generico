import type {Metadata} from "next";import {notFound} from "next/navigation";import {demoPosts} from "@/lib/content";
type Props={params:Promise<{slug:string}>};
export async function generateStaticParams(){return demoPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:Props):Promise<Metadata>{const{slug}=await params;const post=demoPosts.find(p=>p.slug===slug);if(!post)return{};return{title:post.title,description:post.summary,alternates:{canonical:`/blog/${slug}`},openGraph:{type:"article",title:post.title,description:post.summary,publishedTime:post.published_at}}}
export default async function PostPage({params}:Props){const{slug}=await params;const post=demoPosts.find(p=>p.slug===slug);if(!post)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:post.title,datePublished:post.published_at,description:post.summary,author:{"@type":"Organization",name:"Nexo"}};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,"\\u003c")}}/><article className="section page-container prose-page"><p className="eyebrow">{post.category}</p><h1>{post.title}</h1><p className="article-meta"><time dateTime={post.published_at}>{new Intl.DateTimeFormat("es-CL",{dateStyle:"long"}).format(new Date(post.published_at!))}</time></p><p className="hero-lead">{post.summary}</p><h2>Diseñar para el contexto</h2><p>Una arquitectura efectiva no es la que acumula más herramientas, sino la que reduce fricción sin cerrar caminos futuros. En el Edge, cada decisión debe considerar latencia, seguridad y límites del entorno.</p><h2>Principios aplicables</h2><ul><li>Mantener datos y permisos cerca de su fuente de verdad.</li><li>Entregar HTML útil antes de cargar interactividad.</li><li>Medir rendimiento con usuarios y regiones reales.</li></ul><p>Este proyecto convierte esos principios en una base reproducible y documentada.</p></article></main>}
