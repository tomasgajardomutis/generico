import type {Metadata} from "next";import {notFound} from "next/navigation";import {getPostBySlug,getPosts} from "@/lib/content";
type Props={params:Promise<{slug:string}>};
export async function generateStaticParams(){return(await getPosts()).map(post=>({slug:post.slug}))}
export async function generateMetadata({params}:Props):Promise<Metadata>{const{slug}=await params;const post=await getPostBySlug(slug);if(!post)return{};return{title:post.seo_title??post.title,description:post.seo_description??post.summary,alternates:{canonical:`/blog/${slug}`},openGraph:{type:"article",title:post.title,description:post.summary,publishedTime:post.published_at}}}
export default async function PostPage({params}:Props){const{slug}=await params;const post=await getPostBySlug(slug);if(!post)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:post.title,datePublished:post.published_at,description:post.summary,author:{"@type":"Person",name:post.author_name??"Equipo Nexo"}};const paragraphs=post.body?.split(/\n{2,}/).filter(Boolean)??[];return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,"\\u003c")}}/><article className="section page-container prose-page"><p className="eyebrow">{post.category}</p><h1>{post.title}</h1><p className="article-meta"><time dateTime={post.published_at}>{new Intl.DateTimeFormat("es-CL",{dateStyle:"long"}).format(new Date(post.published_at!))}</time> · {post.author_name??"Equipo Nexo"}</p><p className="hero-lead">{post.summary}</p>{paragraphs.map((paragraph,index)=><p key={index}>{paragraph}</p>)}</article></main>}
