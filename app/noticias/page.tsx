import type {Metadata} from "next";import {getNews} from "@/lib/content";
export const metadata:Metadata={title:"Noticias",description:"Novedades y actualizaciones de Nexo.",alternates:{canonical:"/noticias"}};
export default async function NewsPage(){const news=await getNews();return <main><header className="page-hero page-container"><p className="eyebrow">Noticias</p><h1>Lo nuevo en Nexo.</h1><p>Actualizaciones de producto, tecnología y crecimiento.</p></header><section className="section page-container"><div className="card-grid">{news.map(item=><article className="content-card" key={item.id}><p className="article-meta"><time dateTime={item.published_at}>{new Intl.DateTimeFormat("es-CL",{dateStyle:"long"}).format(new Date(item.published_at!))}</time></p><h2>{item.title}</h2><p>{item.summary}</p></article>)}</div></section></main>}
