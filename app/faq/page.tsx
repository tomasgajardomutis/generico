import type {Metadata} from "next";import {getFaqs} from "@/lib/content";
export const metadata:Metadata={title:"Preguntas frecuentes",description:"Respuestas sobre tecnología, seguridad, despliegue y administración.",alternates:{canonical:"/faq"}};
export default async function FaqPage(){const faqs=await getFaqs();const schema={"@context":"https://schema.org","@type":"FAQPage",mainEntity:faqs.map(f=>({"@type":"Question",name:f.question,acceptedAnswer:{"@type":"Answer",text:f.answer}}))};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,"\\u003c")}}/><header className="page-hero page-container"><p className="eyebrow">FAQ</p><h1>Respuestas directas.</h1><p>Todo lo esencial para comprender cómo funciona esta base digital.</p></header><section className="section page-container faq-list">{faqs.map(faq=><details className="faq-item" key={faq.id}><summary>{faq.question}</summary><p>{faq.answer}</p></details>)}</section></main>}
